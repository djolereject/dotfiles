---
### vars file for djole_dev
root_dotfiles: "~/.dotfiles/"
paths:
  oh_my_zsh: "{{ root_dotfiles + '.oh-my-zsh' }}"
  zshrc: "{{ root_dotfiles + '.zshrc' }}"
  emacsd: "{{ root_dotfiles + '.emacs.d' }}"
software:
  apt:
    - git
    - zsh
    - httpie    
    - silversearcher-ag
    - vlc
    - slack
    - firefox
    - emacs26
  brew:
    - git
    - zsh
    - httpie    
    - openssl
    - coreutils
    - the_silver_searcher
    - tree
    - bat
    - fd
    - noti
  cask:
    - qlstephen
    - vlc
    - firefox
    - skype
    - slack    
    - emacs
    - viber
    - telegram
