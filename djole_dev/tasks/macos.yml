---
### tasks specific to OSX
- name: Update Homebrew
  homebrew:
    update_homebrew: yes
- name: Install packages with Brew
  homebrew: name="{{ software.brew }}"
- name: Install packages with Brew Cask
  homebrew_cask: name="{{ software.cask }}"
